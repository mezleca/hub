<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>elter-hub</title>
    <style>
        body, html {
            display: flex;
            width: 100%;
            height: 100%;
            overflow: hidden;
            justify-content: center;
            align-items: center ;
        }
        img, video {
            display: block;
            height: 100vh;
            width: auto;
        }
    </style>
</head>
<body>
    <% const is_mp4 = image[0].format == "mp4"; %>
    <% if (is_mp4) { %>
        <video src="data:video/mp4;base64, <%- image[0].data %>" alt="<%- image[0].name %>" controls=""></video>   
    <% } else { %>
        <img src="data:image/png;base64, <%- image[0].data %>" alt="<%- image[0].name %>"></img>  
    <% } %>
</body>
</html>